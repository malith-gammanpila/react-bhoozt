import { keyframes } from 'styled-components';

export const keyFrame = keyframes`
  0% {
    box-shadow:
      0 0 0 0 rgba(255,0,0, 0.3);
  }
  100% {
    box-shadow:
    0 0 0 15em rgba(255,0,0, 0.0);
  }
`;

export const keyFrameGameBack = keyframes`
  0% {
    box-shadow:
      0 0 0 0 rgba(255,0,0, 0.3);
  }
  100% {
    box-shadow:
    0 0 0 4em rgba(255,0,0, 0.0);
  }
`;
